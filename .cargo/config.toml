[unstable]
build-std = ["core"]
panic-immediate-abort = true

[target.{{ chip-arch }}]
runner = "probe-rs run --chip {{ chip-name }} --connect-under-reset"

[env]
DEFMT_LOG = "trace"

[build]
target = "{{ chip-arch }}"
